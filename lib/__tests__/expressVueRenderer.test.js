const assert = require('assert');
const expressVueRenderer = require('../index.js');

describe('expressVueRenderer', function () {
  it('has a test', function () {
    assert(false, 'expressVueRenderer should have a test');
  });
});
